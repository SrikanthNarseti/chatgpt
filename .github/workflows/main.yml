name: sample workflow
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: writr index.html file
        run: | 
        
          ls -ltr
          sudo cp ./actions-runner/_work/chatgpt/chatgpt/index.html /usr/share/nginx/html/

      
