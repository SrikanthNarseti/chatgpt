name: sample workflow
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: writr index.html file
        run: |
          echo "<h1> this changes comes from pipeline<h1>" > /usr/share/nginx/html/index.html

      
